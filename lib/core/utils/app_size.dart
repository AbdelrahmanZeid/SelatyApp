import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppSize {
  double getWidth(double width) => width.w;
  double getHeight(double height) => height.h;
  double getFontSize(double fontSize) => fontSize.sp;
}
